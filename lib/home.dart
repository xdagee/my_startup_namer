import 'package:flutter/material.dart';

class MyHomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('My Startup Namer'),
      ),
      body: Center(child: Text('Welcome to my Startup Namer app')),
    );
  }
}